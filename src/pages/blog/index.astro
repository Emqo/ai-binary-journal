---
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import Header from '../../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../../consts';
import { getPublishedPosts, listTags } from '../../lib/blog';

const posts = await getPublishedPosts();
const tags = listTags(posts).slice(0, 12);
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			.intro {
				display: flex;
				flex-wrap: wrap;
				align-items: center;
				justify-content: space-between;
				gap: 1rem;
				margin-bottom: 1rem;
			}
			.intro p {
				margin: 0;
				color: var(--ink-soft);
			}
			.tags {
				display: flex;
				flex-wrap: wrap;
				gap: 0.45rem;
			}
			.tags a {
				text-decoration: none;
			}
			.post-list {
				display: grid;
				gap: 0.85rem;
				margin: 0;
				padding: 0;
				list-style: none;
			}
			.post-item a {
				display: block;
				text-decoration: none;
				color: inherit;
			}
			.post-item h3 {
				margin-bottom: 0.55rem;
			}
			.post-item p {
				margin-bottom: 0.75rem;
				color: var(--ink-soft);
			}
			.tag-inline {
				display: flex;
				gap: 0.4rem;
				flex-wrap: wrap;
				margin-top: 0.7rem;
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section>
				<div class="intro">
					<div>
						<h1>博客</h1>
						<p>按时间发布，优先记录 AI 与 Binary 交叉学习中的可复现实验。</p>
					</div>
					<div class="tags">
						{tags.map(({ tag, count }) => <a class="chip" href={`/tags/${tag}/`}>#{tag} ({count})</a>)}
					</div>
				</div>
				<ul class="post-list">
					{
						posts.map((post) => (
							<li class="card post-item">
								<a href={`/blog/${post.id}/`}>
									<h3>{post.data.title}</h3>
									<p>{post.data.description}</p>
									<div class="meta-line">
										<FormattedDate date={post.data.pubDate} />
										<span class="chip" data-kind="accent">{post.data.difficulty}</span>
										{post.data.series && <span class="chip">系列: {post.data.series}</span>}
									</div>
									<div class="tag-inline">
										{post.data.tags.map((tag) => <span class="chip">#{tag}</span>)}
									</div>
								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
